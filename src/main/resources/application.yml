micronaut:
  application:
    name: carros
datasources:
  default:
    url: jdbc:mysql://localhost:3307/carros?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    schema-generate: AUTO
    dialect: MYSQL
jpa.default.properties.hibernate.hbm2ddl.auto: update
